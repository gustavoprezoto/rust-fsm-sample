[package]
name = "fsm_sample"
version = "0.1.0"
edition = "2024"

[dependencies]
rust-fsm = "0.8.0"
tokio = { version = "1.37", features = ["rt-multi-thread", "macros", "sync"] }

[features]
default = ["landi"]

landi = []
positivo = []